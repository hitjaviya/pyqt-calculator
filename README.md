```
1. Create a virtual environment `virtualenv venv`
2. And activate it `source venv/bin/activate`(for ubuntu) `venv\Scripts\activate`(for windows)
3. Then change the directory to PyQt-Calculator and install dependencies `pip install -r requirements.txt`
4. After that run python app.py
```
